spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/point_meter_detection?serverTimezone=GMT%2B8&charset=utf8mb4&useSSL=true
    username: root
    password: 2482510236

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
